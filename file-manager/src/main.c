#include "main.h"

int main(){

    // init();
    // data_cluster d = read_cluster(9);
    // char c1[] = "yan";
    // strncpy((char*)d.dir[0].filename,c1,17*sizeof(char));
    // d.dir[0].first_block = 10;
    // write_cluster(9,d);
    // // d = read_cluster(9);
    // // printf("%s\n", (char*)d.dir[0].filename);

    shell();
    // char c[] = "/yan";
    // char **path = parserStr(c,"/");
    // printf("%d\n",find_cluster(path,2));
    

    return 0;
}